---
phase: 10-v2-polish-hardening
plan: "04"
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - src/hooks/useGameNavigation.ts
  - src/hooks/useExploreMode.ts
  - src/hooks/useStockfishWorker.ts
  - src/hooks/useGameAnalysis.ts
  - src/app/(app)/games/[id]/GameView.tsx
  - src/hooks/useStockfish.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "GameView.tsx delegates board navigation state to useGameNavigation hook"
    - "GameView.tsx delegates explore mode state and handlers to useExploreMode hook"
    - "useStockfish.ts delegates raw UCI worker communication to useStockfishWorker"
    - "useStockfish.ts delegates higher-level analysis state to useGameAnalysis"
    - "GameView.tsx renders correctly after refactor: navigation, explore mode, and analysis panels unchanged from user perspective"
    - "useStockfish public API (analyzeGame, analyzeSinglePosition, terminate) is unchanged"
  artifacts:
    - path: "src/hooks/useGameNavigation.ts"
      provides: "currentMove state + navigation handlers (first/prev/next/last/goToMove)"
      exports: ["useGameNavigation"]
    - path: "src/hooks/useExploreMode.ts"
      provides: "exploreMode state, explorationChess, explorationMoves, exploreEvalResult, explore handlers"
      exports: ["useExploreMode"]
    - path: "src/hooks/useStockfishWorker.ts"
      provides: "Raw worker creation, UCI init, position analysis primitives"
      exports: ["useStockfishWorker"]
    - path: "src/hooks/useGameAnalysis.ts"
      provides: "analyzeGame higher-level state management using useStockfishWorker"
      exports: ["useGameAnalysis"]
  key_links:
    - from: "src/app/(app)/games/[id]/GameView.tsx"
      to: "src/hooks/useGameNavigation.ts"
      via: "const { currentMove, goToMove, goFirst, goPrev, goNext, goLast } = useGameNavigation(totalMoves)"
      pattern: "useGameNavigation"
    - from: "src/app/(app)/games/[id]/GameView.tsx"
      to: "src/hooks/useExploreMode.ts"
      via: "const { exploreMode, ... } = useExploreMode({ ... })"
      pattern: "useExploreMode"
    - from: "src/hooks/useStockfish.ts"
      to: "src/hooks/useStockfishWorker.ts"
      via: "internal use inside useStockfish or useGameAnalysis"
      pattern: "useStockfishWorker"
---

<objective>
Refactor GameView.tsx (871 lines) and useStockfish.ts (622 lines) by extracting focused hooks, reducing file size and coupling.

Purpose: Both files are oversized and hard to test (CONCERNS.md §2.4, §2.6). GameView mixes board state, explore mode, keyboard navigation, Stockfish integration, and LLM panel orchestration in one component. useStockfish manages Web Workers, UCI protocol, best-move parsing, and full-game analysis state. Per D-08, extract into focused hooks that can be developed and tested independently.

Output: 4 new hook files + updated GameView.tsx + reduced useStockfish.ts. Public API of useStockfish unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@src/app/(app)/games/[id]/types.ts
</context>

<interfaces>
<!-- Extracted from GameView.tsx: navigation state to move into useGameNavigation -->
```typescript
// Navigation state (lines ~83-84, ~100-103):
const [currentMove, setCurrentMove] = useState(-1);
const totalMoves = parsed?.positions.length ?? 0;
// Handlers scattered through GameView — first/prev/next/last/goTo handlers
// that update currentMove and optionally call setExploreMode(false)
```

<!-- Extracted from GameView.tsx: explore mode state to move into useExploreMode -->
```typescript
// Explore state (lines ~89-93):
const [exploreMode, setExploreMode] = useState(false);
const [explorationChess, setExplorationChess] = useState<Chess | null>(null);
const [explorationMoves, setExplorationMoves] = useState<ExploreMove[]>([]);
const [exploreEvalResult, setExploreEvalResult] = useState<ExploreEvalResult | null>(null);
const [exploreAnalyzing, setExploreAnalyzing] = useState(false);
// runExploreAnalysis, handleBoardDrop, handleReturnToMainline handlers
```

<!-- useStockfish current public API (must be preserved exactly): -->
```typescript
// From src/hooks/useStockfish.ts bottom of file:
export function useStockfish(): {
  analyzeGame: (positions: GamePosition[], onProgress: (pct: number) => void, signal: AbortSignal) => Promise<EngineAnalysisJsonV1>;
  analyzeSinglePosition: (fen: string) => Promise<ExploreEvalResult>;
  terminate: () => void;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract useGameNavigation hook from GameView (D-08a partial)</name>
  <files>
    src/hooks/useGameNavigation.ts
    src/app/(app)/games/[id]/GameView.tsx
  </files>
  <read_first>
    src/app/(app)/games/[id]/GameView.tsx
    src/app/(app)/games/[id]/types.ts
  </read_first>
  <action>
    Read the full GameView.tsx to identify all navigation-related state and handlers. Navigation state includes: `currentMove` (number, initial -1), `totalMoves` (derived from parsed positions).

    Create `src/hooks/useGameNavigation.ts` with a `useGameNavigation` hook that accepts `{ totalMoves: number }` and returns:
    - `currentMove: number`
    - `goFirst: () => void` — sets currentMove to -1
    - `goPrev: () => void` — decrements currentMove, min -1
    - `goNext: () => void` — increments currentMove, max totalMoves-1
    - `goLast: () => void` — sets currentMove to totalMoves-1
    - `goToMove: (index: number) => void` — clamps to [-1, totalMoves-1]
    - `resetToStart: () => void` — alias for goFirst, used when leaving explore mode

    The hook uses `useState` for `currentMove` and `useCallback` for each handler. Mark the file `"use client"`.

    In `GameView.tsx`:
    - Import `useGameNavigation` from `@/hooks/useGameNavigation`
    - Replace the `const [currentMove, setCurrentMove] = useState(-1)` and all inline navigation handlers with the hook call:
      `const { currentMove, goFirst, goPrev, goNext, goLast, goToMove, resetToStart } = useGameNavigation({ totalMoves });`
    - Replace all inline `setCurrentMove(...)` calls in navigation contexts (buttons, keyboard handler) with the corresponding named handlers.
    - For the keyboard `useEffect` (arrow keys), replace `setCurrentMove((m) => ...)` with `goPrev()` / `goNext()` / `goFirst()` / `goLast()` as appropriate.
    - `setCurrentMove` may still be needed in other contexts (e.g., when explore mode resets) — use `goToMove` or `resetToStart` there.

    Do NOT change any rendering or other state. Verify: the component still renders the same JSX structure.
  </action>
  <verify>
    `Select-String -Path "src/hooks/useGameNavigation.ts" -Pattern "export function useGameNavigation"` returns a match.
    `Select-String -Path "src/app/(app)/games/[id]/GameView.tsx" -Pattern "useGameNavigation"` returns a match.
    `Select-String -Path "src/app/(app)/games/[id]/GameView.tsx" -Pattern "useState\(-1\)"` returns no match (replaced by hook).
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - `src/hooks/useGameNavigation.ts` exists, has `"use client"` directive, exports `useGameNavigation`
    - Hook returns `{ currentMove, goFirst, goPrev, goNext, goLast, goToMove, resetToStart }`
    - `GameView.tsx` no longer defines `const [currentMove, setCurrentMove] = useState(-1)` inline
    - `GameView.tsx` imports and calls `useGameNavigation({ totalMoves })`
    - Keyboard navigation handler in GameView uses named callbacks from the hook
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>useGameNavigation hook extracted; GameView delegates all board navigation state to it.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useExploreMode hook from GameView (D-08a partial)</name>
  <files>
    src/hooks/useExploreMode.ts
    src/app/(app)/games/[id]/GameView.tsx
  </files>
  <read_first>
    src/app/(app)/games/[id]/GameView.tsx
    src/app/(app)/games/[id]/types.ts
  </read_first>
  <action>
    Read the full GameView.tsx to identify all explore-mode state and handlers. Explore state includes: `exploreMode`, `explorationChess`, `explorationMoves`, `exploreEvalResult`, `exploreAnalyzing`, `exploreAnalysisRequestRef`.

    Create `src/hooks/useExploreMode.ts` with a `useExploreMode` hook that accepts:
    ```
    {
      getMainlineFen: () => string;
      analyzeSinglePosition: (fen: string) => Promise<ExploreEvalResult>;
      onEnterExplore?: () => void;  // called when explore mode is entered
      onExitExplore?: () => void;   // called when explore mode is exited
    }
    ```

    Returns:
    - `exploreMode: boolean`
    - `explorationChess: Chess | null`
    - `explorationMoves: ExploreMove[]`
    - `exploreEvalResult: ExploreEvalResult | null`
    - `exploreAnalyzing: boolean`
    - `enterExploreMode: () => void` — sets exploreMode true, initializes Chess from mainline FEN, triggers analysis
    - `exitExploreMode: () => void` — resets all explore state
    - `handleBoardDrop: (sourceSquare: string, targetSquare: string) => boolean` — apply move in explore mode
    - `runExploreAnalysis: (fen: string) => Promise<void>` — request position analysis with request-ID race guard

    Move the `exploreAnalysisRequestRef` into the hook. Keep `promotion: "q"` in handleBoardDrop (the hardcoded behavior, with a comment: `// D-09: queen-only promotion — see separate fix`).

    In `GameView.tsx`:
    - Import `useExploreMode` from `@/hooks/useExploreMode`
    - Replace all explore state and handlers with the hook call
    - The `getMainlineFen` callback (already a `useCallback` in GameView) is passed to the hook
    - `analyzeSinglePosition` from `useStockfish()` is passed to the hook

    Mark `src/hooks/useExploreMode.ts` as `"use client"`.
  </action>
  <verify>
    `Select-String -Path "src/hooks/useExploreMode.ts" -Pattern "export function useExploreMode"` returns a match.
    `Select-String -Path "src/app/(app)/games/[id]/GameView.tsx" -Pattern "useExploreMode"` returns a match.
    `Select-String -Path "src/app/(app)/games/[id]/GameView.tsx" -Pattern "useState.*exploreMode\b"` returns no match.
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - `src/hooks/useExploreMode.ts` exists, has `"use client"` directive, exports `useExploreMode`
    - Hook manages all explore state internally: `exploreMode`, `explorationChess`, `explorationMoves`, `exploreEvalResult`, `exploreAnalyzing`
    - `GameView.tsx` no longer has inline `useState(false)` for `exploreMode` or inline `handleBoardDrop` function body
    - GameView uses `enterExploreMode` / `exitExploreMode` / `handleBoardDrop` from the hook
    - `promotion: "q"` hardcoded behavior preserved (not changed in this task)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>useExploreMode hook extracted; GameView delegates all explore mode state and handlers to it.</done>
</task>

<task type="auto">
  <name>Task 3: Split useStockfish into useStockfishWorker + useGameAnalysis (D-08c)</name>
  <files>
    src/hooks/useStockfishWorker.ts
    src/hooks/useGameAnalysis.ts
    src/hooks/useStockfish.ts
  </files>
  <read_first>
    src/hooks/useStockfish.ts
  </read_first>
  <action>
    Read all 622 lines of `src/hooks/useStockfish.ts`. Identify the split boundary:
    - **Low-level / UCI layer**: worker creation, `waitForInit`, `analyzePosition`, timer management helpers (`track`/`clear`), worker lifecycle. These go into `useStockfishWorker`.
    - **High-level / analysis state layer**: the game-analysis queue, `analyzeGame` orchestration (looping over positions, computing classifications, building `EngineAnalysisJsonV1`), and `analyzeSinglePosition` (explore mode). These go into `useGameAnalysis`.

    Create `src/hooks/useStockfishWorker.ts`:
    - Mark `"use client"`.
    - Export `useStockfishWorker()` that returns:
      - `createAnalysisWorker(): { worker: Worker; waitForReady: (signal?: AbortSignal) => Promise<void>; analyzePosition: (fen: string, signal?: AbortSignal) => Promise<RawResult>; terminate: () => void }`
      - `createExploreWorker(): { same shape }`
    - Move the module-level `waitForInit` and `analyzePosition` helper functions into this file as non-exported helpers used by the hook internals.
    - The `track`/`clear` timer management helpers move here.

    Create `src/hooks/useGameAnalysis.ts`:
    - Mark `"use client"`.
    - Export `useGameAnalysis(workerFactory: ReturnType<typeof useStockfishWorker>)` that returns `{ analyzeGame, analyzeSinglePosition, terminate }` — identical shapes to the current `useStockfish` return.
    - This is where the game-loop logic (iterating positions, classifying moves, emitting progress) lives.

    Update `src/hooks/useStockfish.ts`:
    - Reduce to a thin composition hook that calls `useStockfishWorker()` and `useGameAnalysis(workerFactory)` and returns the combined public API:
      ```
      export function useStockfish() {
        const workerFactory = useStockfishWorker();
        return useGameAnalysis(workerFactory);
      }
      ```
    - The public API (`analyzeGame`, `analyzeSinglePosition`, `terminate`) MUST be identical to the current API — GameView.tsx imports `useStockfish` unchanged.

    Move types `CandidateMove`, `GamePosition`, `RawResult` to `src/hooks/useStockfishWorker.ts` and re-export from `useStockfish.ts` for backwards compat.

    Do NOT change the `"use client"` directive on `useStockfish.ts`.
  </action>
  <verify>
    `Select-String -Path "src/hooks/useStockfishWorker.ts" -Pattern "export function useStockfishWorker"` returns a match.
    `Select-String -Path "src/hooks/useGameAnalysis.ts" -Pattern "export function useGameAnalysis"` returns a match.
    `(Get-Item "src/hooks/useStockfish.ts").length` is less than before the refactor (the file is now a thin wrapper).
    `Select-String -Path "src/hooks/useStockfish.ts" -Pattern "useStockfishWorker"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
    `npx.cmd vitest run` passes.
  </verify>
  <acceptance_criteria>
    - `src/hooks/useStockfishWorker.ts` exists with `"use client"` and exports `useStockfishWorker`
    - `src/hooks/useGameAnalysis.ts` exists with `"use client"` and exports `useGameAnalysis`
    - `src/hooks/useStockfish.ts` is reduced to a composition wrapper; no longer contains raw Worker instantiation or UCI protocol code
    - `useStockfish()` return type is identical to before: `{ analyzeGame, analyzeSinglePosition, terminate }`
    - `GameView.tsx` requires zero changes to its `useStockfish()` usage (import path and destructuring unchanged)
    - TypeScript compilation passes
    - All Vitest tests pass
  </acceptance_criteria>
  <done>useStockfish split into useStockfishWorker (raw UCI) + useGameAnalysis (state) + thin useStockfish wrapper; public API unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stockfish Web Worker→useStockfishWorker | Worker messages are string-based UCI; no sanitization needed (internal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | Queen-promotion hardcode in useExploreMode | accept | D-09 fix is handled in Plan 05; placeholder comment left in code |
| T-10-11 | Denial of Service | Explore analysis race condition | mitigate | Request ID guard (`exploreAnalysisRequestRef`) moved into useExploreMode hook; preserved from GameView |
</threat_model>

<verification>
`npx.cmd tsc --noEmit` — zero errors.
`npx.cmd vitest run` — all tests pass.
Line count check: `(Get-Content "src/hooks/useStockfish.ts").Count` should be substantially less than 622 (expect < 50 lines for the thin wrapper).
Line count check: `(Get-Content "src/app/(app)/games/[id]/GameView.tsx").Count` should be noticeably less than 871 (expect < 650 after 2 hook extractions).
</verification>

<success_criteria>
- 4 new hook files created: `useGameNavigation.ts`, `useExploreMode.ts`, `useStockfishWorker.ts`, `useGameAnalysis.ts`
- `GameView.tsx` line count reduced (navigation + explore state extracted)
- `useStockfish.ts` line count reduced to thin wrapper
- `useStockfish()` public API unchanged — GameView imports/uses it identically
- TypeScript + Vitest pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-v2-polish-hardening/10-04-SUMMARY.md`
</output>
