---
phase: 10-v2-polish-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0002_rls_policies.sql
  - src/app/api/games/[id]/engine-analysis/route.ts
  - src/app/api/chess-accounts/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "RLS is enabled on all 7 product tables with service-role bypass policies in migration 0002"
    - "POST /api/games/[id]/engine-analysis rejects bodies larger than 500 KB before calling req.json()"
    - "POST /api/chess-accounts rejects Lichess usernames not matching [a-zA-Z0-9_-] and Chess.com usernames not matching [a-zA-Z0-9_]"
  artifacts:
    - path: "drizzle/0002_rls_policies.sql"
      provides: "RLS enable + service-role bypass policies for all product tables"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "src/app/api/games/[id]/engine-analysis/route.ts"
      provides: "Content-Length guard before req.json()"
      contains: "content-length"
    - path: "src/app/api/chess-accounts/route.ts"
      provides: "Platform-specific username regex validation"
      contains: "LICHESS_USERNAME_RE"
  key_links:
    - from: "drizzle/0002_rls_policies.sql"
      to: "Neon DB"
      via: "drizzle-kit push or manual apply"
      pattern: "CREATE POLICY"
    - from: "src/app/api/chess-accounts/route.ts"
      to: "trimmedUsername"
      via: "regex test before validateUsernameExists"
      pattern: "LICHESS_USERNAME_RE|CHESS_COM_USERNAME_RE"
---

<objective>
Security hardening pass: enable RLS on all product tables, guard the engine-analysis endpoint against oversized payloads, and validate chess platform usernames before making external API calls.

Purpose: Close the three highest-priority security gaps identified in CONCERNS.md §1.2, §1.5, and §6.3. All product tables currently rely solely on application-layer ownership checks; RLS adds a DB-layer safety net. The payload and username guards prevent degenerate input from reaching expensive code paths.

Output: Migration file `drizzle/0002_rls_policies.sql`, updated `engine-analysis/route.ts`, updated `chess-accounts/route.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RLS migration for all product tables (D-01)</name>
  <files>drizzle/0002_rls_policies.sql</files>
  <read_first>
    drizzle/0001_align_auth_schema.sql
    src/db/schema.ts
  </read_first>
  <action>
    Create `drizzle/0002_rls_policies.sql` following the `-->statement-breakpoint` separator style used in `0001_align_auth_schema.sql`.

    The migration must enable RLS on the 7 product tables and add a service-role bypass policy to each. The 7 tables are: `chess_accounts`, `games`, `engine_analyses`, `game_analyses`, `group_analyses`, `player_summaries`, `llm_request_locks`.

    Pattern for each table (repeat for all 7):
    - `ALTER TABLE "public"."<table>" ENABLE ROW LEVEL SECURITY;`
    - `CREATE POLICY "<table>_service_role_bypass" ON "public"."<table>" TO service_role USING (true) WITH CHECK (true);`

    Use `service_role` as the Postgres role name — this is the Neon superuser role that the Next.js backend connects as. The policy USING (true) WITH CHECK (true) grants unrestricted read/write to the service role, while RLS blocks all other roles by default.

    Do NOT add user-scoped policies (`current_setting('app.current_user_id')`); those belong to v2 Phase 3. Do NOT modify the auth tables (`users`, `auth_accounts`, `auth_verification_tokens`) — they already have RLS enabled via 0001.

    Add a comment block at the top of the file:
    `-- 0002: Enable RLS + service-role bypass on product tables (Phase 10 security hardening)`
    `-- Auth tables (users, auth_accounts, auth_verification_tokens) were handled in 0001.`

    This migration must be applied manually to Neon by running `npx.cmd drizzle-kit push` or via the Neon console — do NOT auto-run it; just create the file.
  </action>
  <verify>
    File `drizzle/0002_rls_policies.sql` exists. Running:
    `Select-String -Path "drizzle/0002_rls_policies.sql" -Pattern "ENABLE ROW LEVEL SECURITY"` returns 7 matches (one per product table).
    `Select-String -Path "drizzle/0002_rls_policies.sql" -Pattern "CREATE POLICY"` returns 7 matches.
    None of the 7 matches reference `users`, `auth_accounts`, or `auth_verification_tokens`.
  </verify>
  <acceptance_criteria>
    - File exists at `drizzle/0002_rls_policies.sql`
    - Exactly 7 `ENABLE ROW LEVEL SECURITY` statements — one per product table: `chess_accounts`, `games`, `engine_analyses`, `game_analyses`, `group_analyses`, `player_summaries`, `llm_request_locks`
    - Exactly 7 `CREATE POLICY` statements with `TO service_role USING (true) WITH CHECK (true)`
    - Auth tables (`users`, `auth_accounts`, `auth_verification_tokens`) are NOT present
    - File uses `-->statement-breakpoint` separators consistent with `0001_align_auth_schema.sql`
  </acceptance_criteria>
  <done>Migration file created; 7 RLS enables + 7 service-role bypass policies present; auth tables excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Content-Length guard on engine-analysis POST (D-03)</name>
  <files>src/app/api/games/[id]/engine-analysis/route.ts</files>
  <read_first>
    src/app/api/games/[id]/engine-analysis/route.ts
  </read_first>
  <action>
    In the `POST` handler (after UUID validation and ownership check, before calling `req.json()`), add a Content-Length guard:

    Read `req.headers.get("content-length")` into a constant `contentLength`. If the value is a non-null string, parse it as an integer. If the parsed integer exceeds `MAX_PAYLOAD_BYTES` (define as module-level constant: `const MAX_PAYLOAD_BYTES = 500 * 1024;`), return `NextResponse.json({ error: "Payload too large" }, { status: 413 })` immediately — before calling `req.json()`.

    Insert this check after the `ownedGame` null-check block (after line that returns 404 if game not found) and before the existing `let body: unknown; try { body = await req.json(); }` block.

    If `content-length` header is absent or not parseable as a finite integer, do not reject — allow the request through (the header is optional in some clients).
  </action>
  <verify>
    `Select-String -Path "src/app/api/games/[id]/engine-analysis/route.ts" -Pattern "MAX_PAYLOAD_BYTES"` returns a match.
    `Select-String -Path "src/app/api/games/[id]/engine-analysis/route.ts" -Pattern "content-length"` returns a match.
    `Select-String -Path "src/app/api/games/[id]/engine-analysis/route.ts" -Pattern "413"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - `MAX_PAYLOAD_BYTES` constant defined at module level as `500 * 1024` (512000)
    - `req.headers.get("content-length")` is read and parsed as integer before `req.json()` is called
    - Returns HTTP 413 with `{ error: "Payload too large" }` when Content-Length exceeds `MAX_PAYLOAD_BYTES`
    - Missing or non-numeric Content-Length header does not cause a rejection
    - Guard is placed BEFORE `req.json()` (not after)
    - TypeScript compilation passes (`npx.cmd tsc --noEmit`)
  </acceptance_criteria>
  <done>Content-Length guard added; payloads over 500 KB rejected with 413 before JSON parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Platform-specific username regex validation in chess-accounts (D-04)</name>
  <files>src/app/api/chess-accounts/route.ts</files>
  <read_first>
    src/app/api/chess-accounts/route.ts
  </read_first>
  <action>
    After the existing `username.trim().length > 50` check and before the `validateUsernameExists` call, add platform-specific regex validation.

    Define two module-level constants:
    - `const LICHESS_USERNAME_RE = /^[a-zA-Z0-9_-]{2,50}$/;`
    - `const CHESS_COM_USERNAME_RE = /^[a-zA-Z0-9_]{3,50}$/;`

    (Lichess allows hyphens; Chess.com does not. Lichess minimum is 2 chars; Chess.com minimum is 3.)

    After the length check for `username.trim().length > 50`, insert:
    ```
    if (platform === "lichess" && !LICHESS_USERNAME_RE.test(trimmedUsername)) {
      return error 400 "Некоректний нікнейм для Lichess (дозволені: літери, цифри, _ та -)"
    }
    if (platform === "chess_com" && !CHESS_COM_USERNAME_RE.test(trimmedUsername)) {
      return error 400 "Некоректний нікнейм для Chess.com (дозволені: літери, цифри та _)"
    }
    ```

    The error message must be in Ukrainian consistent with other error messages in this route. Return `NextResponse.json({ error: "..." }, { status: 400 })`.

    `trimmedUsername` is already defined at that point in the existing code — use it (not `username.trim()` again).
  </action>
  <verify>
    `Select-String -Path "src/app/api/chess-accounts/route.ts" -Pattern "LICHESS_USERNAME_RE"` returns a match.
    `Select-String -Path "src/app/api/chess-accounts/route.ts" -Pattern "CHESS_COM_USERNAME_RE"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - `LICHESS_USERNAME_RE = /^[a-zA-Z0-9_-]{2,50}$/` defined at module level
    - `CHESS_COM_USERNAME_RE = /^[a-zA-Z0-9_]{3,50}$/` defined at module level
    - Both regex tests applied to `trimmedUsername` BEFORE the `validateUsernameExists` call
    - Non-matching username returns HTTP 400 with Ukrainian error message
    - Regex tests are placed after the existing string/length validation, not before
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Platform username regex validation added; invalid usernames rejected with Ukrainian 400 message before external API call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST /api/games/[id]/engine-analysis | Unauthenticated or authenticated client sends engine analysis JSON; size not previously bounded |
| client→POST /api/chess-accounts | Authenticated client sends platform + username; username not previously regex-validated |
| Neon DB role→product tables | App DB role can bypass RLS if role has BYPASSRLS; without policies all rows are accessible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service | POST /api/games/[id]/engine-analysis | mitigate | Content-Length guard (D-03): reject > 500 KB before `req.json()` with HTTP 413 |
| T-10-02 | Information Disclosure | Product tables (chess_accounts, games, etc.) | mitigate | RLS enable + service_role bypass policy (D-01): DB enforces isolation independent of app layer |
| T-10-03 | Spoofing | POST /api/chess-accounts username | mitigate | Platform regex (D-04): invalid chars rejected before external API call; prevents misleading errors |
| T-10-04 | Elevation of Privilege | Neon service_role BYPASSRLS | accept | Neon managed service; service_role is the intended app role; documented in migration comment |
</threat_model>

<verification>
After applying the migration to Neon:
- `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public' AND tablename IN ('chess_accounts','games','engine_analyses','game_analyses','group_analyses','player_summaries','llm_request_locks');` — all 7 rows show `rowsecurity = true`
- `SELECT * FROM pg_policies WHERE schemaname='public';` — 7 service_role bypass policies present

For route changes: `npx.cmd tsc --noEmit` passes. Existing Vitest tests pass: `npx.cmd vitest run`.
</verification>

<success_criteria>
- `drizzle/0002_rls_policies.sql` contains 7 ENABLE RLS + 7 CREATE POLICY statements (no auth tables)
- POST /api/games/[id]/engine-analysis returns 413 when Content-Length > 500 KB
- POST /api/chess-accounts returns 400 for `user-name!` on Lichess (invalid char `!`) and `user-name` on Chess.com (invalid char `-`)
- TypeScript compilation (`npx.cmd tsc --noEmit`) passes
- `npx.cmd vitest run` passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-v2-polish-hardening/10-01-SUMMARY.md`
</output>
