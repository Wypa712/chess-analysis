---
phase: 10-v2-polish-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/llm-lock.ts
  - src/lib/llm/groq-client.ts
  - src/app/api/games/[id]/analyze/route.ts
  - src/app/api/analysis/group/route.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "acquireLlmLock and releaseLlmLock exist only in src/lib/db/llm-lock.ts and are imported from both analyze routes"
    - "The Groq client singleton exists only in src/lib/llm/groq-client.ts and is imported from both analyze routes"
    - "POST /api/analysis/group computes inputHash and checks cache BEFORE calling buildGameSummaries"
    - "Both analyze routes compile and pass existing tests after the refactor"
  artifacts:
    - path: "src/lib/db/llm-lock.ts"
      provides: "acquireLlmLock / releaseLlmLock functions"
      exports: ["acquireLlmLock", "releaseLlmLock"]
    - path: "src/lib/llm/groq-client.ts"
      provides: "Groq client singleton"
      exports: ["groq"]
    - path: "src/app/api/games/[id]/analyze/route.ts"
      provides: "Imports llm-lock + groq-client; no local duplicates"
      contains: "from \"@/lib/db/llm-lock\""
    - path: "src/app/api/analysis/group/route.ts"
      provides: "Imports llm-lock + groq-client; cache lookup before buildGameSummaries"
      contains: "from \"@/lib/llm/groq-client\""
  key_links:
    - from: "src/lib/db/llm-lock.ts"
      to: "src/app/api/games/[id]/analyze/route.ts"
      via: "named import"
      pattern: "acquireLlmLock|releaseLlmLock"
    - from: "src/lib/llm/groq-client.ts"
      to: "src/app/api/analysis/group/route.ts"
      via: "named import"
      pattern: "import.*groq.*from.*groq-client"
---

<objective>
Extract duplicate LLM infrastructure into shared libraries and fix the cache-ordering bug in group analysis.

Purpose: The `acquireLlmLock`/`releaseLlmLock` functions and the Groq client singleton are copy-pasted verbatim in two API routes (CONCERNS.md §2.1, §2.2). Any lock behavior change requires editing two files. The group analysis cache bug (CONCERNS.md §3.5) causes `buildGameSummaries` to run on every POST even when a cache hit will immediately discard the result.

Output: Two new shared library files (`src/lib/db/llm-lock.ts`, `src/lib/llm/groq-client.ts`) + updated analyze routes that import from them + fixed cache-ordering in group route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<interfaces>
<!-- Extracted from src/app/api/games/[id]/analyze/route.ts lines 57-84 -->
<!-- These are the exact signatures to replicate in the shared lib: -->

From src/app/api/games/[id]/analyze/route.ts (current duplicate to be extracted):
```typescript
const LOCK_TTL_MS = 2 * 60 * 1000;

async function acquireLlmLock(lockKey: string, userId: string, scope: string): Promise<boolean> {
  await db.delete(llmRequestLocks).where(sql`${llmRequestLocks.expiresAt} < NOW()`);
  const rows = await db.insert(llmRequestLocks).values({
    lockKey, userId, scope,
    expiresAt: new Date(Date.now() + LOCK_TTL_MS),
  }).onConflictDoNothing().returning({ lockKey: llmRequestLocks.lockKey });
  return rows.length > 0;
}

async function releaseLlmLock(lockKey: string, userId: string): Promise<void> {
  try {
    await db.delete(llmRequestLocks)
      .where(and(eq(llmRequestLocks.lockKey, lockKey), eq(llmRequestLocks.userId, userId)));
  } catch (err) {
    console.error("[LLM] lock release failed:", err);
  }
}
```

From src/app/api/games/[id]/analyze/route.ts line 21 (Groq client to extract):
```typescript
const groq = process.env.GROQ_API_KEY
  ? new Groq({ apiKey: process.env.GROQ_API_KEY })
  : null;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract LLM lock helpers into shared lib (D-06)</name>
  <files>src/lib/db/llm-lock.ts</files>
  <read_first>
    src/app/api/games/[id]/analyze/route.ts
    src/db/schema.ts
  </read_first>
  <action>
    Create directory `src/lib/db/` if it does not exist, then create `src/lib/db/llm-lock.ts`.

    The file exports two async functions: `acquireLlmLock` and `releaseLlmLock`. Copy the implementations verbatim from `src/app/api/games/[id]/analyze/route.ts` lines 57–84. Keep the `LOCK_TTL_MS = 2 * 60 * 1000` constant as a module-level constant in this file.

    Imports needed: `db` from `@/db`, `llmRequestLocks` from `@/db/schema`, `and`, `eq`, `sql` from `drizzle-orm`.

    Add a section divider comment at the top: `// ─── LLM Request Lock helpers ───────────────────────────────────────────────`

    Export both functions as named exports (no default export). Add a comment on `acquireLlmLock` explaining the `finally` contract: `// Note: JS finally always runs on return inside try — lock is released on all paths.`

    Do NOT change the function signatures or logic. This is a pure extraction.
  </action>
  <verify>
    `Select-String -Path "src/lib/db/llm-lock.ts" -Pattern "export async function acquireLlmLock"` returns a match.
    `Select-String -Path "src/lib/db/llm-lock.ts" -Pattern "export async function releaseLlmLock"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - File `src/lib/db/llm-lock.ts` exists
    - `acquireLlmLock(lockKey: string, userId: string, scope: string): Promise<boolean>` is exported
    - `releaseLlmLock(lockKey: string, userId: string): Promise<void>` is exported
    - `LOCK_TTL_MS = 2 * 60 * 1000` is defined in this file (not imported)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Shared llm-lock.ts created with acquireLlmLock and releaseLlmLock exported.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Groq client singleton into shared lib (D-07)</name>
  <files>src/lib/llm/groq-client.ts</files>
  <read_first>
    src/app/api/games/[id]/analyze/route.ts
    src/app/api/analysis/group/route.ts
  </read_first>
  <action>
    Create directory `src/lib/llm/` if it does not exist, then create `src/lib/llm/groq-client.ts`.

    The file exports a single named export `groq` which is the Groq client singleton:
    ```
    import Groq from "groq-sdk";
    export const groq = process.env.GROQ_API_KEY
      ? new Groq({ apiKey: process.env.GROQ_API_KEY })
      : null;
    ```

    Add a comment above the export: `// P0-3: module-level client so HTTP connections are reused across warm serverless invocations`

    This matches the existing comment in both analyze routes. The type of `groq` is `Groq | null`.
  </action>
  <verify>
    `Select-String -Path "src/lib/llm/groq-client.ts" -Pattern "export const groq"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
  </verify>
  <acceptance_criteria>
    - File `src/lib/llm/groq-client.ts` exists
    - `export const groq` is `Groq | null` (falsy when `GROQ_API_KEY` is absent)
    - File imports from `"groq-sdk"` (not a relative path)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Shared groq-client.ts created with groq singleton exported.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shared libs into both analyze routes + fix group cache ordering (D-06, D-07, D-12)</name>
  <files>
    src/app/api/games/[id]/analyze/route.ts
    src/app/api/analysis/group/route.ts
  </files>
  <read_first>
    src/app/api/games/[id]/analyze/route.ts
    src/app/api/analysis/group/route.ts
    src/lib/db/llm-lock.ts
    src/lib/llm/groq-client.ts
  </read_first>
  <action>
    In `src/app/api/games/[id]/analyze/route.ts`:
    1. Remove the local `acquireLlmLock` and `releaseLlmLock` function definitions (lines 57–84).
    2. Remove the local `groq` const definition (line 21 — `const groq = process.env.GROQ_API_KEY ? new Groq(...) : null`).
    3. Remove the `import Groq from "groq-sdk"` import (it is no longer instantiated here).
    4. Remove the `LOCK_TTL_MS` constant if it is only used by the removed lock functions.
    5. Add import: `import { acquireLlmLock, releaseLlmLock } from "@/lib/db/llm-lock";`
    6. Add import: `import { groq } from "@/lib/llm/groq-client";`
    7. Keep all other logic unchanged. The `groq` null-check (`if (!groq)`) remains — it now guards the imported value.

    In `src/app/api/analysis/group/route.ts`:
    1. Remove the local `acquireLlmLock` and `releaseLlmLock` function definitions (lines 292–319 approximately).
    2. Remove the local `groq` const definition.
    3. Remove the `import Groq from "groq-sdk"` import.
    4. Remove the local `LOCK_TTL_MS` constant if only used by lock functions.
    5. Add import: `import { acquireLlmLock, releaseLlmLock } from "@/lib/db/llm-lock";`
    6. Add import: `import { groq } from "@/lib/llm/groq-client";`

    Cache ordering fix (D-12) in `src/app/api/analysis/group/route.ts`:
    Currently at line ~104: `const summaries = await buildGameSummaries(ownedGames);` runs before the cache lookup.
    Reorder so the cache lookup (inputHash computation + DB query for `group_analyses`) runs FIRST, and `buildGameSummaries` is only called on cache miss.

    The reordering requires computing `inputHash` before summaries. Currently `inputHash` is derived from `summaries` via `createGroupInputHash(summaries)`. This means `buildGameSummaries` must run before the hash can be computed. To fix the ordering, the inputHash must be computable from the raw game IDs + model + prompt version instead of from summaries.

    Simpler alternative (preferred, lower risk): Keep `buildGameSummaries` where it is but move the cache DB lookup to immediately after `inputHash` is computed (lines 106–133). This eliminates the "summaries computed on cache hit" waste by checking cache right after hash, before the lock acquisition. Even if summaries are still computed, the lock is not acquired and no Groq call is made on cache hit — which is the primary waste.

    Concretely: move the `cachedRows` DB select block and its `if (cachedRows.length > 0 ...)` early return to immediately after `const inputHash = createGroupInputHash(summaries)` — before `acquireLlmLock`. This is already approximately the current position; verify it is before `acquireLlmLock`, not inside the `try` block after it.

    If the cache check IS already before `acquireLlmLock`, add a comment confirming the ordering is correct and no code change is needed for D-12.
  </action>
  <verify>
    `Select-String -Path "src/app/api/games/[id]/analyze/route.ts" -Pattern "from.*llm-lock"` returns a match.
    `Select-String -Path "src/app/api/games/[id]/analyze/route.ts" -Pattern "from.*groq-client"` returns a match.
    `Select-String -Path "src/app/api/games/[id]/analyze/route.ts" -Pattern "^const groq"` returns no match (removed local def).
    `Select-String -Path "src/app/api/games/[id]/analyze/route.ts" -Pattern "^async function acquireLlmLock"` returns no match.
    `Select-String -Path "src/app/api/analysis/group/route.ts" -Pattern "from.*llm-lock"` returns a match.
    `Select-String -Path "src/app/api/analysis/group/route.ts" -Pattern "from.*groq-client"` returns a match.
    `npx.cmd tsc --noEmit` exits 0.
    `npx.cmd vitest run` passes.
  </verify>
  <acceptance_criteria>
    - Neither analyze route contains a local `acquireLlmLock` or `releaseLlmLock` function definition
    - Neither analyze route contains a local `const groq = new Groq(...)` or `new Groq({...})`
    - Neither analyze route imports `from "groq-sdk"` directly
    - Both routes import `{ acquireLlmLock, releaseLlmLock }` from `"@/lib/db/llm-lock"`
    - Both routes import `{ groq }` from `"@/lib/llm/groq-client"`
    - In group route: cache lookup (`cachedRows` DB select) occurs before `acquireLlmLock` is called
    - TypeScript compilation passes
    - All Vitest tests pass
  </acceptance_criteria>
  <done>Both analyze routes import llm-lock and groq-client from shared libs; no local duplicates; group cache lookup confirmed before lock acquisition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared lib→route handlers | Shared lock/client modules must behave identically to the removed inline copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | LLM lock release on early return | accept | JS `finally` always executes on `return` inside `try`; add documentation comment per CONCERNS.md §4.3 |
| T-10-06 | Denial of Service | Group analysis: summaries computed on cache hit | mitigate | Cache lookup moved before lock acquisition (D-12); Groq not called on hit |
</threat_model>

<verification>
`npx.cmd tsc --noEmit` — zero errors.
`npx.cmd vitest run` — all tests pass.
Grep for `new Groq(` in `src/app/api/` — returns zero matches (client only instantiated in shared lib).
Grep for `function acquireLlmLock` in `src/app/api/` — returns zero matches.
</verification>

<success_criteria>
- `src/lib/db/llm-lock.ts` exists and exports `acquireLlmLock` + `releaseLlmLock`
- `src/lib/llm/groq-client.ts` exists and exports `groq`
- Neither analyze route contains local copies of lock helpers or groq client
- Cache lookup in group route precedes lock acquisition
- TypeScript + Vitest both pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-v2-polish-hardening/10-02-SUMMARY.md`
</output>
