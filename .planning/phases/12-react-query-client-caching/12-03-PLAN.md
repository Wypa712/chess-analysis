---
phase: 12-react-query-client-caching
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
files_modified:
  - src/app/(app)/games/[id]/GameView.tsx
autonomous: true
requirements:
  - REQ-V2-7

must_haves:
  truths:
    - "Повторний перехід на вже відвідану сторінку партії не робить нових запитів до /api/games/[id]/engine-analysis та /api/games/[id]/analyze"
    - "Engine analysis завантажується через useQuery з queryKey ['engine-analysis', gameId] та staleTime Infinity"
    - "LLM analysis завантажується через useQuery з queryKey ['llm-analysis', gameId] та staleTime Infinity"
    - "POST запити (Stockfish save, LLM analyze) залишаються plain fetch або useMutation — не змінюють логіку Stockfish worker"
    - "initialFetchCount guard замінено на перевірку статусу useQuery (isPending або isLoading)"
  artifacts:
    - path: "src/app/(app)/games/[id]/GameView.tsx"
      provides: "GameView з useQuery для GET analysis endpoints"
      contains: "useQuery"
  key_links:
    - from: "src/app/(app)/games/[id]/GameView.tsx"
      to: "/api/games/[id]/engine-analysis GET"
      via: "useQuery queryFn"
      pattern: "useQuery.*engine-analysis"
    - from: "src/app/(app)/games/[id]/GameView.tsx"
      to: "/api/games/[id]/analyze GET"
      via: "useQuery queryFn"
      pattern: "useQuery.*llm-analysis"
---

<objective>
Мігрувати GET-запити у GameView (engine analysis + LLM analysis) на useQuery з staleTime Infinity.

Purpose: Повторний перехід на сторінку партії не повторює запити — результати аналізу незмінні після обчислення (per Claude's Discretion у D-07: staleTime Infinity для analysis).
Output: GameView використовує useQuery для двох GET endpoint. Stockfish worker логіка залишається незмінною. POST запити залишаються plain fetch.
</objective>

<execution_context>
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/workflows/execute-plan.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/templates/summary.md
</execution_context>

<context>
@/c/Users/Олександр/Downloads/chess-analysis/.planning/ROADMAP.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/12-CONTEXT.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/12-01-SUMMARY.md

<interfaces>
<!-- Поточний стан GameView.tsx — два useEffect для GET, що ЗАМІНЮЮТЬСЯ на useQuery -->

Поточний engine analysis fetch (рядки 107-124 GameView.tsx) — ЦЕ БУДЕ ЗАМІНЕНО:
```typescript
const [analysisState, setAnalysisState] = useState<"idle"|"loading"|"done"|"error">("idle");
const [analysis, setAnalysis] = useState<EngineAnalysisJsonV1 | null>(null);
const [analysisError, setAnalysisError] = useState<string | null>(null);
const [initialFetchCount, setInitialFetchCount] = useState(0);

useEffect(() => {
  fetch(`/api/games/${game.id}/engine-analysis`)
    .then(r => { if (!r.ok) throw new Error(...); return r.json(); })
    .then(data => {
      if (data?.analysis && isEngineAnalysisJsonV1(data.analysis)) {
        setAnalysis(data.analysis);
        setAnalysisState("done");
      }
    })
    .catch(() => { setAnalysisError(...); setAnalysisState("error"); })
    .finally(() => setInitialFetchCount(c => c + 1));
}, [game.id]);
```

Поточний LLM analysis fetch (рядки 127-140) — ЦЕ БУДЕ ЗАМІНЕНО:
```typescript
const [llmStatus, setLlmStatus] = useState<LlmStatus>("idle");
const [llmAnalysis, setLlmAnalysis] = useState<LlmGameAnalysisV1 | null>(null);

useEffect(() => {
  fetch(`/api/games/${game.id}/analyze`)
    .then(r => (r.ok ? r.json() : null))
    .then(data => {
      if (data?.analysis && isLlmGameAnalysisV1(data.analysis)) {
        setLlmAnalysis(data.analysis);
        setLlmStatus("done");
      }
    })
    .catch(() => setLlmStatus("idle"))
    .finally(() => setInitialFetchCount(c => c + 1));
}, [game.id]);
```

Захист: if (initialFetchCount < 2) return <RouteLoader .../> — ЗАМІНИТИ на перевірку isPending обох queries.

НЕ ЧІПАТИ (per D-09):
- useStockfish() та його виклики (analyzeGame, analyzeSinglePosition, terminate)
- handleStartAnalysis — Stockfish worker + POST до /api/games/[id]/engine-analysis
- handleLlmAnalyze — POST до /api/games/[id]/analyze
- useExploreMode, useGameNavigation

Типи що залишаються:
- LlmStatus: "idle" | "analyzing" | "done" | "error" — залишити для POST flow (handleLlmAnalyze)
- analysisState: "idle" | "loading" | "done" | "error" — залишити для Stockfish worker progress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Замінити GET useEffect на useQuery у GameView</name>
  <files>src/app/(app)/games/[id]/GameView.tsx</files>
  <read_first>
    - src/app/(app)/games/[id]/GameView.tsx — повний файл: обидва useEffect для GET, useState для analysis/llm, initialFetchCount guard, handleStartAnalysis та handleLlmAnalyze (POST — НЕ ЧІПАТИ)
    - src/lib/chess/engine-analysis.ts — переконатись у сигнатурі isEngineAnalysisJsonV1 та EngineAnalysisJsonV1 типу
    - src/lib/llm/types.ts — переконатись у сигнатурі isLlmGameAnalysisV1 та LlmGameAnalysisV1 типу
  </read_first>
  <action>
    Мігрувати два GET useEffect на два useQuery виклики. Зміни тільки у цьому файлі.

    КРОК 1 — Додати імпорти:
    Додати useQuery з "@tanstack/react-query" до існуючого рядка імпортів.

    КРОК 2 — Замінити engine analysis GET useEffect:
    Замість useState(analysis)+useEffect fetch, використати useQuery:
    - queryKey: ['engine-analysis', game.id]
    - queryFn: async ({ signal }) => { const r = await fetch(`/api/games/${game.id}/engine-analysis`, { signal }); if (!r.ok) throw new Error(...); const d = await r.json(); return d?.analysis && isEngineAnalysisJsonV1(d.analysis) ? d.analysis : null; }
    - staleTime: Infinity
    - retry: 1

    Результат: const { data: engineAnalysisData, isPending: enginePending, isError: engineIsError } = useQuery(...)
    Потім: const analysis = engineAnalysisData ?? null

    КРОК 3 — Замінити LLM analysis GET useEffect:
    - queryKey: ['llm-analysis', game.id]
    - queryFn: async ({ signal }) => { const r = await fetch(`/api/games/${game.id}/analyze`, { signal }); if (!r.ok) return null; const d = await r.json(); return d?.analysis && isLlmGameAnalysisV1(d.analysis) ? d.analysis : null; }
    - staleTime: Infinity
    - retry: 0 (LLM може повернути null якщо аналіз не готовий — не треба retry)

    Результат: const { data: llmAnalysisData, isPending: llmPending } = useQuery(...)
    Потім: const llmAnalysis = llmAnalysisData ?? null

    КРОК 4 — Оновити початковий стан:
    - Видалити: useState для analysis (тепер з useQuery data)
    - Видалити: useState для llmAnalysis (тепер з useQuery data)
    - Видалити: initialFetchCount useState + setInitialFetchCount виклики у обох useEffect
    - Видалити: обидва GET useEffect (залишити інші useEffect: keyboard handler, ResizeObserver, terminate)

    КРОК 5 — Оновити initialFetchCount guard:
    Замінити `if (initialFetchCount < 2)` на `if (enginePending || llmPending)`:
    if (enginePending || llmPending) {
      return <RouteLoader text="Завантажуємо партію…" />;
    }

    КРОК 6 — Оновити analysisState при GET результаті:
    Початковий стан analysisState залежить від того чи є engineAnalysisData:
    - useState("idle") → useState(engineAnalysisData ? "done" : "idle")
    АБО ініціалізувати analysisState у useEffect при зміні engineAnalysisData:
    useEffect(() => {
      if (engineAnalysisData) setAnalysisState("done");
    }, [engineAnalysisData]);

    КРОК 7 — Оновити llmStatus початковий стан:
    Аналогічно: useState(llmAnalysisData ? "done" : "idle") для llmStatus.

    КРОК 8 — handleStartAnalysis залишити незмінним:
    handleStartAnalysis виконує analyzeGame (Stockfish worker) + POST до engine-analysis API.
    Після successful POST можна інвалідувати queryClient.invalidateQueries(['engine-analysis', game.id]) — але це опціонально: оскільки staleTime Infinity, без інвалідації кеш не оновиться. Тому: додати useQueryClient() і після успішного POST зробити invalidateQueries або setQueryData для ['engine-analysis', game.id].
    Використати queryClient.setQueryData(['engine-analysis', game.id], result) замість invalidateQueries — це уникне зайвого мережевого запиту і оновить кеш напряму з result (результат вже є в пам'яті).

    КРОК 9 — handleLlmAnalyze POST залишити незмінним:
    handleLlmAnalyze виконує POST fetch. Після успішного POST: setLlmAnalysis(data.analysis) + setLlmStatus("done") — ці useState залишаються для POST flow управління.
    Додатково: queryClient.setQueryData(['llm-analysis', game.id], data.analysis) щоб при наступній навігації кеш був актуальний.

    ОБМЕЖЕННЯ: НЕ змінювати useStockfish виклики, useExploreMode, useGameNavigation, useLayoutEffect для ResizeObserver.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('src/app/(app)/games/[id]/GameView.tsx','utf8'); if (!c.includes('useQuery')) { console.error('useQuery missing'); process.exit(1); } if (c.includes('initialFetchCount')) { console.error('initialFetchCount still present'); process.exit(1); } if (!c.includes(\"'engine-analysis'\")) { console.error('engine-analysis queryKey missing'); process.exit(1); } if (!c.includes(\"'llm-analysis'\")) { console.error('llm-analysis queryKey missing'); process.exit(1); } if (!c.includes('staleTime: Infinity')) { console.error('staleTime Infinity missing'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - GameView.tsx містить два useQuery виклики з queryKey ['engine-analysis', game.id] та ['llm-analysis', game.id]
    - Обидва useQuery мають staleTime: Infinity
    - GameView.tsx не містить "initialFetchCount" ніде
    - Захист від рендерингу до завершення fetch: enginePending || llmPending умова перед return основного JSX
    - handleStartAnalysis та handleLlmAnalyze (POST flows) залишаються з plain fetch — useStockfish НЕ змінено
    - setQueryData викликається після успішного POST для ['engine-analysis', game.id] та ['llm-analysis', game.id]
    - GameView.tsx компілюється без TypeScript помилок
  </acceptance_criteria>
  <done>GameView кешує результати аналізу. Повторний перехід на ту саму сторінку партії не робить нових мережевих запитів до engine-analysis та analyze endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/games/[id]/engine-analysis | GET запит авторизований через session cookie |
| client→/api/games/[id]/analyze | GET/POST запити авторизовані через session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | queryKey ['engine-analysis', gameId] | accept | gameId не є секретом; кеш локальний в браузері; API незалежно перевіряє що партія належить поточному user |
| T-12-03-02 | Tampering | setQueryData після POST | accept | Дані для setQueryData приходять з API response — той самий рівень довіри що і перед міграцією |
| T-12-03-03 | Denial of Service | staleTime Infinity | accept | Infinity означає ніколи не рефетчить автоматично; POST + setQueryData контролює оновлення; прийнятно бо analysis незмінний після обчислення |
</threat_model>

<verification>
1. Перейти на /games/[id] вперше — два запити до engine-analysis та analyze видно у Network tab
2. Повернутись на /dashboard, знову перейти на той же /games/[id] — Network tab НЕ показує нових запитів до цих endpoints
3. Запустити аналіз через "Запустити аналіз" — Stockfish worker працює як раніше
4. GameView рендериться без помилок TypeScript (перевірити через npm.cmd run build або tsc)
</verification>

<success_criteria>
- Engine та LLM analysis GET endpoints кешуються з staleTime Infinity
- Повторна навігація на вже відвіданий gameId не робить нових GET запитів
- Stockfish worker та handleStartAnalysis незмінні (D-09 виконано)
- POST flows (handleStartAnalysis POST save, handleLlmAnalyze) працюють як раніше
</success_criteria>

<output>
Після завершення створити `.planning/phases/12-react-query-client-caching/12-03-SUMMARY.md`
</output>
