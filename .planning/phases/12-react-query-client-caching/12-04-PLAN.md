---
phase: 12-react-query-client-caching
plan: 04
type: execute
wave: 3
depends_on:
  - 12-01-PLAN.md
  - 12-02-PLAN.md
files_modified:
  - src/components/ProfileView/ProfileView.tsx
autonomous: true
requirements:
  - REQ-V2-7

must_haves:
  truths:
    - "GET /api/analysis/group у ProfileView замінено на useQuery з queryKey ['group-analysis']"
    - "Повторний перехід на /profile не робить нового запиту до /api/analysis/group якщо дані свіжі"
    - "POST /api/analysis/group (handleGroupAnalyze) залишається plain fetch, після успіху оновлює кеш через setQueryData"
    - "groupLoading/groupReanalyzing/groupError UI стан збережено (деградація при помилці лишається)"
  artifacts:
    - path: "src/components/ProfileView/ProfileView.tsx"
      provides: "ProfileView з useQuery для group analysis GET"
      contains: "useQuery"
  key_links:
    - from: "src/components/ProfileView/ProfileView.tsx"
      to: "/api/analysis/group GET"
      via: "useQuery queryFn"
      pattern: "useQuery.*group-analysis"
---

<objective>
Мігрувати GET /api/analysis/group у ProfileView на useQuery — останній client-side fetch що залишився поза React Query кешем після планів 02 і 03.

Purpose: Завершує міграційний scope (D-10). Повторна навігація на /profile не повторює group analysis запит.
Output: ProfileView використовує useQuery для GET group analysis. POST flow залишається plain fetch + setQueryData для оновлення кешу.
</objective>

<execution_context>
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/workflows/execute-plan.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/templates/summary.md
</execution_context>

<context>
@/c/Users/Олександр/Downloads/chess-analysis/.planning/ROADMAP.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/12-CONTEXT.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/12-01-SUMMARY.md
@/c/Users/Олександр/Downloads/chess-analysis/.planning/phases/12-react-query-client-caching/12-02-SUMMARY.md

<interfaces>
<!-- Поточний стан ProfileView.tsx — GET useEffect що ЗАМІНЮЄТЬСЯ на useQuery -->

Поточний group analysis GET (рядки 40-61 ProfileView.tsx) — ЦЕ БУДЕ ЗАМІНЕНО:
```typescript
const [groupAnalysis, setGroupAnalysis] = useState<GroupAnalysisRow | null>(null);
const [groupLoading, setGroupLoading] = useState(false);

useEffect(() => {
  setGroupLoading(true);
  fetch("/api/analysis/group")
    .then(res => { if (!res.ok) throw new Error(...); return res.json(); })
    .then(data => {
      if (data?.analysis && isGroupAnalysisJsonV1(data.analysis.analysisJson)) {
        setGroupAnalysis(data.analysis);
      } else {
        setGroupAnalysis(null);
      }
      setGroupLoading(false);
    })
    .catch(err => { setGroupError(...); setGroupLoading(false); });
}, []);
```

Поточний POST (рядки 63-87) — залишити як plain fetch, лише додати setQueryData після успіху:
```typescript
async function handleGroupAnalyze() {
  setGroupReanalyzing(true);
  // ... fetch("/api/analysis/group", { method: "POST" })
  // Після успіху: setGroupAnalysis(data.analysis) — ЗАМІНИТИ на queryClient.setQueryData(...)
}
```

GroupAnalysisRow тип:
```typescript
type GroupAnalysisRow = {
  id: string;
  gameIds: string[];
  analysisJson: GroupAnalysisJsonV1;
  createdAt: string;
};
```

QueryKey для group analysis: ['group-analysis']
(Немає userId в ключі — group analysis глобальна для юзера, auth на API рівні)
staleTime: 10 * 60 * 1000 (10 хвилин — group analysis рідко змінюється, але не Infinity бо може бути перегенерована)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Мігрувати group analysis GET на useQuery у ProfileView</name>
  <files>src/components/ProfileView/ProfileView.tsx</files>
  <read_first>
    - src/components/ProfileView/ProfileView.tsx — повний файл: GROUP analysis useEffect (рядки ~40-61), handleGroupAnalyze POST (рядки ~63-87), GroupAnalysisRow тип, groupLoading/groupReanalyzing/groupError стани, де groupAnalysis використовується у JSX (рядки ~340+)
    - src/lib/llm/types.ts — isGroupAnalysisJsonV1 та GroupAnalysisJsonV1 типи (потрібні для queryFn)
  </read_first>
  <action>
    Мігрувати тільки GET fetch групового аналізу на useQuery. POST залишається без змін (крім setQueryData).

    КРОК 1 — Додати імпорти:
    Додати useQuery, useQueryClient з "@tanstack/react-query" до існуючих імпортів.

    КРОК 2 — Замінити GET useEffect на useQuery:
    Видалити useState для groupAnalysis та groupLoading.
    Додати:
    const queryClient = useQueryClient()
    const {
      data: groupAnalysisData,
      isPending: groupLoading,
      isError: groupFetchError,
    } = useQuery({
      queryKey: ['group-analysis'],
      queryFn: async ({ signal }) => {
        const res = await fetch('/api/analysis/group', { signal });
        if (!res.ok) throw new Error('Failed to fetch group analysis');
        const data = await res.json();
        if (data?.analysis && isGroupAnalysisJsonV1(data.analysis.analysisJson)) {
          return data.analysis as GroupAnalysisRow;
        }
        return null;
      },
      staleTime: 10 * 60 * 1000,
      retry: 1,
    });
    const groupAnalysis = groupAnalysisData ?? null;

    КРОК 3 — Оновити groupError логіку:
    groupFetchError з useQuery замінює catch-блок useEffect для GET.
    Залишити: groupError useState для POST помилок (handleGroupAnalyze може встановити groupError).
    Об'єднати: const hasGroupError = groupFetchError || !!groupError;
    АБО залишити окремі перевірки у JSX для GET-помилки та POST-помилки.

    КРОК 4 — Оновити handleGroupAnalyze POST:
    Залишити fetch POST незмінним. Після успіху замінити setGroupAnalysis(data.analysis) на:
    queryClient.setQueryData(['group-analysis'], data.analysis as GroupAnalysisRow);
    Видалити setGroupAnalysis виклик з POST handler (якщо groupAnalysis тепер з useQuery).

    КРОК 5 — Видалити старий useEffect для GET:
    Видалити весь useEffect що робив fetch("/api/analysis/group") GET.
    Видалити setGroupLoading(true) та відповідний setState.

    КРОК 6 — Перевірити JSX використання groupAnalysis:
    GroupAnalysisPanel отримує analysis={groupAnalysis.analysisJson} тощо — перевірити що groupAnalysis тип сумісний (GroupAnalysisRow | null).
    groupLoading тепер isPending з useQuery — перевірити всі місця де groupLoading використовується у JSX умовах.

    НЕ ЧІПАТИ:
    - useProfileStats hook та statsStats/initialLoading/refetching
    - Весь інший JSX ProfileView (ELO графіки, статистика, фільтр по днях)
    - handleGroupAnalyze POST fetch логіка (крім setQueryData замість setGroupAnalysis)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('src/components/ProfileView/ProfileView.tsx','utf8'); if (!c.includes('useQuery')) { console.error('useQuery missing'); process.exit(1); } if (!c.includes(\"'group-analysis'\")) { console.error('group-analysis queryKey missing'); process.exit(1); } const hasOldEffect = /useEffect\(\s*\(\)\s*=>\s*\{[\s\S]*?fetch\(['\"]\\/api\\/analysis\\/group['\"]\)[\s\S]*?\},\s*\[\s*\]\s*\)/.test(c); if (hasOldEffect) { console.error('old GET useEffect still present'); process.exit(1); } if (!c.includes('setQueryData')) { console.error('setQueryData missing in POST handler'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - ProfileView.tsx містить useQuery з queryKey ['group-analysis']
    - staleTime дорівнює 10 * 60 * 1000 або 600000
    - Старий GET useEffect (що fetch("/api/analysis/group") з [] deps) більше не присутній у файлі
    - handleGroupAnalyze після успішного POST викликає queryClient.setQueryData(['group-analysis'], ...) замість setGroupAnalysis(...)
    - groupLoading у JSX коректно відображає стан завантаження (isPending з useQuery)
    - ProfileView компілюється без TypeScript помилок
    - GroupAnalysisPanel отримує коректні пропи з groupAnalysis (GroupAnalysisRow | null)
  </acceptance_criteria>
  <done>Group analysis GET кешується через React Query. Повторна навігація на /profile не повторює запит протягом 10 хвилин. Після перегенерації (POST) кеш оновлюється через setQueryData.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/analysis/group | GET/POST запити авторизовані через session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | queryKey ['group-analysis'] без userId | accept | Group analysis API повертає тільки дані поточного юзера (auth на API рівні); queryKey без userId прийнятний бо кеш локальний до браузер-сесії |
| T-12-04-02 | Tampering | setQueryData після POST | accept | Дані для setQueryData приходять з API response — той самий рівень довіри що і без кешування |
</threat_model>

<verification>
1. Перейти на /profile — group analysis запит виконується
2. Перейти на /dashboard, повернутись на /profile — Network tab НЕ показує нового запиту до /api/analysis/group протягом 10 хвилин
3. Натиснути кнопку перегенерації group analysis — POST виконується, результат відображається без перезавантаження сторінки
4. ProfileView рендериться без React помилок у консолі
</verification>

<success_criteria>
- GET /api/analysis/group кешується з staleTime 10 хвилин
- POST flow (handleGroupAnalyze) оновлює кеш через setQueryData без додаткового GET запиту
- Всі фази міграції (D-08, D-10) виконані: DashboardClient/GamesList, GameView, ProfileView group analysis
- TypeScript компілюється без нових помилок
</success_criteria>

<output>
Після завершення створити `.planning/phases/12-react-query-client-caching/12-04-SUMMARY.md`
</output>
